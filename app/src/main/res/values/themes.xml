<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicClubClassic" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.MusicClubClassic" parent="Base.Theme.MusicClubClassic">
        <item name="cardViewStyle">@style/AppCardViewStyle</item>
        <item name="toolbarStyle">@style/AppToolBar</item>
        <item name="android:navigationBarColor">@color/bg_bottom_bar</item>
        <item name="imageButtonStyle">@style/BaseImageButton</item>
        <item name="android:windowBackground">@color/app_background_color</item>
    </style>
</resources>